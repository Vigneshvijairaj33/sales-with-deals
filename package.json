{
  "name": "discount-aggregator",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/mobile"
  ],
  "scripts": {
    "api": "npm run dev --workspace=apps/api",
    "test": "npm run test --workspace=apps/api"
  }
}
